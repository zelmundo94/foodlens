import React from 'react';
import PropTypes from "prop-types";

// class Recipes extends React.Component {

// render() {


  const AnotherRecipeButton = ( props ) => {
    return (
     <div className="recipeDiv">
      <p>{props.title}</p>
     </div>
    )
   };
   
  

  export default AnotherRecipeButton;
